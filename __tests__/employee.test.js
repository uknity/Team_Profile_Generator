const index = require('../index');
const employee = require('../lib/employee');

describe("employee information and determine what type of employee", () => {
    it("what type of employee is it?", () => {

    });

    describe("it will assign the employee to the correct subclass file and send data to the corresponding js file", () => {

    })
}